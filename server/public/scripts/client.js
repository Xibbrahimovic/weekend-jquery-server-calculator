$(onReady);

function onReady(){
    console.log('jquery loaded!');

    //click listeners\
    $(`.addButton`).on(`click`, add)
    $(`.subtractButton`).on(`click`, subtract)
    $(`.subtractButton`).on(`click`, mutliply)
    $(`.subtractButton`).on(`click`, )


}